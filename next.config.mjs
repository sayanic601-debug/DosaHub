/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Standard domains to allow loading sample food images
    domains: ['images.unsplash.com'],
  },
};

export default nextConfig;
